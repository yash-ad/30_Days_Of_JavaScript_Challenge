
// 1. Default Export
// Problem:
// Create a JavaScript module file named greetings.js that contains a single function:
// greet(name): Returns a greeting message "Hello, [name]!".

export function greet(name) {
return `Hello,${name} Hows it going!`  
}